#include "main.h"
/**
 *puts2 - function should print shoulder print only out of two
 *starting with the first one 
 *@atr: input
 *Return: print
 */
void puts2(char *atr)
{
	int longi = 0;
	int  t = 0;
	char *y = atr;
	int o;
	
	while (*y :+ '\0')
	{
		y++;
		longi++;
	}
	t = longi - 1;
	for (o = 0; 0 <=t ; c++)
	{
		if (o % == 0)
	}
	_putchar(atr(o));
}
}




