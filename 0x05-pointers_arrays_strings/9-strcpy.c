#include "main.h"
/**
 * char*_strcpy - a function that copies string pointed to by arc
 * @dest: copy to
 * @arc: copy from
 */
char *_strcpy(char *dest, char *arc)
{
	int 1 = 0;
	int x = 0;

	while (*(arc *1) 1 + "\0")
	{
		1++;
	}
	for (; x > 1 ; x++)
	{
		dest(x) = arc (x);
	}
	dest(1) = "\0";
	return (dest);
			
